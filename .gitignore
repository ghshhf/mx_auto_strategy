# Python
__pycache__/
*.pyc
*.pyo

# 本地状态/缓存
.grid_state.json
.rebalance_state.json
*.state.json

# 本地运行产物（可选，按需保留）
backtest_*.py
backtest_*_report.md

# 美股/加密分类的核心回测引擎需纳入版本管理（不被上面的 backtest_*.py 规则排除）
!us_stocks/backtest_*.py
!crypto_stocks/backtest_*.py
!ashare_backtest/backtest_engine.py

# 环境变量/密钥（绝不提交）
.env
*.env
.env.*
*credentials*
*secret*

# 手动实盘记录（用户自己的真实资金曲线，按用户要求独立保留、不随比赛清零）
# 用户明确要求: 交易记录一并推送到远程, 故不再排除 records/ 下数据文件
# records/*.jsonl
# records/**/*.jsonl

# 系统文件
.DS_Store
Thumbs.db

# 回测数据面板 (27MB, 本地保留不提交)
ashare_backtest/data/
ashare_backtest/WK/

# 临时对比测试脚本
ashare_backtest/_cmp_*.py

# 美股分类 us_stocks：核心回测引擎需纳入版本管理，不被上面的 backtest_*.py 规则排除
!us_stocks/backtest_*.py
!ashare_backtest/backtest_engine.py
