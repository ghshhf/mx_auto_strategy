# Python
__pycache__/
*.pyc
*.pyo

# 本地状态/缓存
.grid_state.json
.rebalance_state.json
*.state.json

# 本地运行产物（可选，按需保留）
backtest_*.py
backtest_*_report.md

# 环境变量/密钥（绝不提交）
.env
*.env
.env.*
*credentials*
*secret*

# 手动实盘记录（用户自己的真实资金曲线，按用户要求独立保留、不随比赛清零）
# 仅排除数据文件，保留 records/ 目录结构（.gitkeep 占位），供 clone 的 AI 知道往哪写
records/*.jsonl

# 系统文件
.DS_Store
Thumbs.db

# 公募基金真实持仓层缓存(妙想刷新, 运行时生成, 不提交)
fund_holdings_cache.json
stock_industry_cache.json
_full_refresh.log
