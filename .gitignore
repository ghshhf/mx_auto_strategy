# Python
__pycache__/
*.pyc
*.pyo

# 本地状态/缓存
.grid_state.json
.rebalance_state.json
*.state.json

# 本地运行产物（仅限仓库根目录的一次性脚本）
# ⚠️ 必须用前导 / 锚定到仓库根: 早期这里的 `backtest_*.py` 是全局模式,
#    会把 markets/ashare/、markets/crypto/research/ 等任意子目录下的同名文件
#    **静默忽略**(git status 不显示、提交时无声丢失), 只能靠 ! 白名单逐个放行,
#    而白名单又只匹配顶层目录, 子目录仍会漏。锚定后该陷阱整体消失。
/backtest_*.py
/backtest_*_report.md

# 环境变量/密钥（绝不提交）
.env
*.env
.env.*
*credentials*
*secret*

# 手动实盘记录（用户自己的真实资金曲线，按用户要求独立保留、不随比赛清零）
# 用户明确要求: 交易记录一并推送到远程, 故不再排除 records/ 下数据文件
# records/*.jsonl
# records/**/*.jsonl

# 系统文件
.DS_Store
Thumbs.db

# 回测数据面板 (历史大文件, 本地保留不提交)
markets/ashare/WK/
# A股重建面板 (腾讯后复权周线; 由 markets/ashare/tencent_hfq_rebuild.py 本地生成, 不入库)
# 缺失时基线完整性 / 周期叠加等测试会 skip, 属预期行为(跑一次 rebuild 即恢复)
markets/ashare/data/ashare_weekly_em/
markets/ashare/data/ashare_panel_close_em.csv
markets/ashare/data/ashare_panel_volume_em.csv
# 注: markets/ashare/data/ashare (A股日线/资金流缓存, 需上传GitHub作公共数据源) 不忽略, 正常提交

# 旧的瞬时节点的缓存(已迁移到 data/ashare 并随仓库提交)
markets/ashare/_cache/
markets/ashare/cache/

# 临时对比测试脚本
markets/ashare/_cmp_*.py
