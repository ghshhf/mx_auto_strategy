# Crypto 策略倍率 · 真值声明（2026-09-10 重跑 · **27 币池** · 期权层已关闭）

> 目的：终结「倍率对不上 / 减着减着变奇怪」的困惑，并清理期权时代遗留数字。
> 结论：**所有数字都真实，差异 100% 来自「窗口长度」与「引擎版本 / 配置」，不是 bug。**
> 验证脚本：`markets/crypto/reconcile_truth.py`（12y / 10y）、`markets/crypto/export_nav_crypto.py`（发布 NAV）。
> 均用受管 python `E:/xmanbian/_venv/mx_quant/Scripts/python.exe` 跑通、可复现。

## ⚠️ 期权三件套已于 2026-08-31 临时关闭

审计（2026-08-17）发现 `crypto_options_bt.py` 的 **put 保险层被误建模为收益引擎**（赔付 > 成本被当成正收益，真实崩盘保险是要花钱的），且叠加 `alloc_offense_mult=1.2` 后被复利放大，导致旧头条 `59.361Mx / MDD −23.8%` 量级虚高 5~6 个数量级。

- 关闭后 `crypto_options_bt.py` 的期权四开关（call / put / short / cycle-gate）全部降级为无操作路径，逐腿拆解验证：**关 call / 关 put / 关做空，倍率变化恒为 Δ=+0.0%**（见 §1.3）。
- 因此本文**不再引用任何「期权增强」倍率**；当前头条是 **现货轮动 + 减半相位叠加**。

## 0. 唯一权威真值（当前引擎，**27 币面板**，期权关闭）

> 🔄 **2026-09-10 重跑（当前）**：面板 32 → **27 币**（终版池，`_screen_current_pool.json` 的 `panel=v3(27)`）。
> 🔄 2026-09-08 池变更：32 → 27 币。旧 32 币数字（发布 NAV 5,662.51x）**已作废**。
> 🔄 2026-09-06 池变更：删 ICP + INJ（34 → 32 币）。旧 34 币数字（发布 NAV 7,637.77x / 10y FULL 8,488x / 12y FULL 26,211x）**已作废，不可再引用**。

| 口径 | 倍率 | MDD | Sharpe | CAGR |
|---|---|---|---|---|
| **发布 NAV**（cycle_overlay, tilt=0.3, 10y） | **5,630.11x** | −70.4% | 1.60 | 135.2% |
| reconcile 10y FULL（inv_vol+1.2+周期） | 6,059x | −70.5% | 1.60 | 136.9% |
| reconcile 12y FULL | 18,711x | −70.5% | 1.56 | 127.0% |

> 发布 NAV（`docs/data/nav_crypto.json`，供 `docs/index.html` 展示）与 reconcile 10y FULL 倍率接近（5,630x vs 6,059x），差异来自分配机制（cycle_overlay tilt=0.3 vs inv_vol+1.2），MDD 一致 −70.4~70.5%。**两者都是现真值，引用时务必标清口径。**

## 1. 权威真值表（当前引擎 · **27 币面板**，期权已关闭）

### 1.1 10 年窗口（2016-08-12 ~ 2026-09-04，526 周）
| 配置 | 10y 倍率 | MDD | Sharpe | CAGR |
|---|---|---|---|---|
| FULL（inv_vol+1.2+周期） | **6,059x** | −70.5% | 1.60 | 136.9% |
| MULT1.0（inv_vol+1.0+周期） | 1,228x | −67.8% | 1.52 | 102.3% |
| EQUAL（等权+1.0+周期） | 1,068x | −68.1% | 1.50 | 99.5% |
| NO_CYCLE（inv_vol+1.2, 关周期） | 1,019x | −80.2% | 1.31 | 98.6% |
| NO_CYCLE_MULT1.0（纯现货轮动） | 284x | −76.8% | 1.23 | 75.0% |

### 1.2 12 年窗口（2014-09-19 ~ 2026-09-04，625 周）
| 配置 | 12y 倍率 | MDD | Sharpe | CAGR |
|---|---|---|---|---|
| FULL（inv_vol+1.2+周期） | **18,711x** | −70.5% | 1.56 | 127.0% |
| MULT1.0 | 2,946x | −67.8% | 1.49 | 94.6% |
| EQUAL | 2,563x | −68.1% | 1.47 | 92.3% |
| NO_CYCLE | 3,147x | −80.2% | 1.31 | 95.7% |
| NO_CYCLE_MULT1.0（纯现货轮动） | 681x | −76.8% | 1.23 | 72.2% |

> 📌 **32→27 币的影响高度集中在「关周期」口径**：FULL 几乎不变（6,030x → 6,043x），
> 但 NO_CYCLE 10y 从 2,407x **腰斩到 1,055x**、12y 从 7,433x → 3,257x。
> 含义：被删的 5 个币主要贡献的是「不依赖减半相位的普涨 beta」，而相位叠加的 alpha 不受池缩影响。
> **引用任何「关周期」数字时务必标注币池版本**，否则会差 2 倍以上。

> 🔧 **2026-09-10 面板尾部快照修正（同为 27 币，本表已更新为修正后值）**：
> 同步脚本原先只追加「晚于面板末日」的行，而**当前未完结周一旦写入就永不更新** ——
> 若某次同步恰好跑在周初，该周行会永久冻结在周初价格。实测 2026-08-30 那周
> RAY 周内 +66%（0.772 → 1.286），面板却记成 0.778（**−39%**）；ZEC −33%、UNI −30% 同类。
> 修正方式：同步改为**回溯重写尾部 8 周**（已收盘周用交易所终值覆盖），
> 共修正 93 格，最大 39.5%；复核残留偏差 ≤0.20%（仅剩进行中那一周的日内漂移）。
> 影响：FULL 口径几乎不动（10y 6,043x → **6,059x**，+0.3%），
> 但**「关周期」口径对近期数据更敏感**：10y NO_CYCLE 1,055x → **1,019x**（−3.4%）、
> NO_CYCLE_MULT1.0 292x → **284x**。今后引用关周期数字须同时标注**币池版本 + 面板修正日期**。

### 1.3 逐腿拆解（验证期权层已彻底关闭，10y FULL 基础上关单腿）
| 关单腿 | 倍率 | Δ | 结论 |
|---|---|---|---|
| 关 covered call | 6,059x | +0.0% | ✓ 无贡献 |
| 关 put 保险 | 6,059x | +0.0% | ✓ 无贡献 |
| 关 做空 | 6,059x | +0.0% | ✓ 无贡献 |

## 2. 为什么之前「对不上」（根因拆解）

| 差异来源 | 例子 | 量级 |
|---|---|---|
| **窗口长度** | 同一引擎 12y(18,711x) vs 10y(6,059x) | 3.1× |
| **配置叠加** | cycle_overlay(5,630x) vs inv_vol+1.2(6,059x) | ~1.08× |
| **期权时代废弃数字** | 59.36Mx（期权开）/ 28,092x（43币+期权）/ 24,493x（旧 DEFAULT） | 不可比，已废弃 |

**核心：项目曾未固定「唯一窗口 + 唯一引擎版本 + 唯一期权状态」，所以同一引擎被报成 24,493x / 28,092x / 59.36Mx / 448.6x 等。这些都不是当前真值，只是实验口径。**

## 3. 已推翻的假设（重要）

- ❌ 原猜「put 保险是最大收益腿，关掉 −100%」
- ✅ 审计证实 put 层被误建模为收益引擎，属 bug 而非真实 alpha；**关闭后倍率不变（Δ=+0.0%）**，旧 59.36Mx / 448.6x 数字作废。
- ✅ 减半周期相位减仓是真实、合法、零后视的 alpha（见 `docs/CYCLE_DERISK.md`）；关闭周期（NO_CYCLE）倍率显著下降且 MDD 恶化到 −79%，印证其有效性。

## 4. 不随窗口变化的稳定结论

- **减半周期相位减仓有效（且 27 币下更强）**：关周期（NO_CYCLE）10y 6,059x→1,019x（−83%），MDD −70.5%→−80.2%。
- **alloc_offense_mult 1.2 放大显著**：10y 6,059x vs 1,228x（1.0）= 4.94×；12y 18,711x vs 2,946x = 6.35×。
- **加密 MDD 是结构性难题**：即便加周期减仓，MDD 仍 −70.4%（加密单周 −20~−40% 常态），要再压只能降进攻权重。

## 5. 待办（防止再次碎片化）

- [x] `reconcile_truth.py` 重写：固定面板（**2026-09-10 起为 27 币**）+ 10y/12y 双窗口 + 期权关闭验证。
- [x] `export_nav_crypto.py` 产出发布 NAV（**5,630.11x**，27 币池），供站点展示。
      ⚠️ 该脚本曾把 `source` 里的币数**硬编码为 "34币"**，面板早已变 27 币而发布页长期挂着错误口径；
      已改为从 `px.shape[1]` 动态推导，并加 `n_coins` 字段 + `tests/test_crypto_truth_consistency.py` 断言。
- [x] README / PITFALLS / docs 统一引用本真值表，标注「窗口 + 配置 + 期权状态」。
- [x] 新增 **`run_all.py`**（2026-09-10）单次产出 crypto/us/ashare/组合层全部权威结果 + 数字回归断言。
      用法：`python run_all.py`（比对 `reports/truth_baseline.json`，超容差 exit 1）、
      `--baseline`（改池/修数据后刷新基线）、`--only us,crypto,blend`、`--no-deep`。
      A股面板 gitignore 时会标 `NO_DATA` 跳过而非失败。CI 已接入。
      **改池/改引擎/修数据后：先跑 run_all → 若红则同步本文件与 docs/TRUTH.md → 再 --baseline。**
- [ ] 期权层若未来重做（真对称赔付 + 样本外验证），须独立 PR 并标注新真值，不可直接复用旧 59.36Mx。
