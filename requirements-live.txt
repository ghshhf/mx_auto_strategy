# mx_auto_strategy — 可选依赖：实时/历史行情拉取
#
# CI 刻意不装本节（ci.yml / publish.yml 只装 requirements.txt），原因：
#   1. 这两个包在代码里**全部是函数内惰性 import**，且各自都有兜底数据源；
#   2. akshare 依赖树很重，装它会显著拖慢 CI 并增加失败面。
#
# 缺失时的行为（不会 ImportError，只是少一条取数路径）：
#   akshare  -> ashare_backtest 的 A股指数日线 / ETF 资金流 / 估值分位
#               兜底：东财 push2delay 接口 (etf_fund_flow)、腾讯行情 (analog_core)
#               入口：analog_core._pull_akshare_index / etf_fund_flow(AK_OK 开关)
#                     / macro_fetch / valuation_fetch
#   yfinance -> crypto_btc_eth 的 Yahoo 日线 (data_sources._yahoo_via_yfinance)
#               兜底：/v8/finance/chart 原始 REST，无需任何第三方库
#
# 本地安装：pip install -r requirements-live.txt
akshare>=1.12
yfinance>=0.2
